apiVersion: apps/v1

kind: StatefulSet

metadata:
  name: {{ .Values.statefulset.mysqlslave.name }}

  labels:
{{ .Values.labels | toYaml | indent 4 }}

spec:
  selector:
    matchLabels:
{{ .Values.labels | toYaml | indent 6 }}

  serviceName: {{ .Values.service.mysqlslave.name }}

  replicas: {{ .Values.statefulset.mysqlslave.replicas }}

  updateStrategy:
{{ .Values.statefulset.mysqlslave.updateStrategy | toYaml | indent 4 }}

  template:
    metadata:
      labels:
{{ .Values.labels | toYaml | indent 8 }}

    spec:
      nodeSelector:
{{ .Values.statefulset.mysqlslave.pod.nodeSelector | toYaml | indent 8 }}

      containers:
      - name: {{ .Values.statefulset.mysqlslave.name }}

        image: "{{ .Values.statefulset.mysqlslave.pod.container.image.repo }}:{{ .Values.statefulset.mysqlslave.pod.container.image.version }}"

        imagePullPolicy: {{ .Values.statefulset.mysqlslave.pod.container.image.pullPolicy }}

        ports:
        - containerPort: {{ .Values.statefulset.mysqlslave.pod.container.port }}

        command:
{{ .Values.statefulset.mysqlslave.pod.container.command | toYaml | indent 10 }}

        env:
{{ .Values.statefulset.mysqlslave.pod.container.env | toYaml | indent 10 }}

        volumeMounts:
{{ .Values.statefulset.mysqlslave.pod.container.volumeMounts | toYaml | indent 10 }}

  volumeClaimTemplates:
{{ .Values.statefulset.mysqlslave.volumeClaimTemplates | toYaml | indent 4 }}