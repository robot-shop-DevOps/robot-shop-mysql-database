apiVersion: batch/v1

kind: Job

metadata:
  name: {{ .Values.job.mysqlmaster.name }}
  labels:
{{ .Values.labels | toYaml | indent 4 }}

spec:
  template:
    spec:
      restartPolicy: {{ .Values.job.mysqlmaster.pod.restartPolicy }}

      nodeSelector: 
{{ .Values.job.mysqlmaster.pod.nodeSelector | toYaml | indent 8 }}

      containers:
{{ .Values.job.mysqlmaster.pod.containers | toYaml | indent 8 }}