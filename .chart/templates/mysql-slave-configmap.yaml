apiVersion: v1

kind: ConfigMap

metadata:
  name: {{ .Values.configMap.mysql-slave.name }}
  labels:
{{ .Values.labels | toYaml | indent 4 }}

data:
{{ .Values.configMap.mysql-slave.data | toYaml | indent 2 }}