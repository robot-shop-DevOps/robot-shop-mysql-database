apiVersion: v1

kind: ConfigMap

metadata:
  name: {{ .Values.configMap.mysqlslave.name }}
  labels:
{{ .Values.labels | toYaml | indent 4 }}

data:
{{ .Values.configMap.mysqlslave.data | toYaml | indent 2 }}