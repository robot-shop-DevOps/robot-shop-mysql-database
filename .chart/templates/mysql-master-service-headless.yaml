apiVersion: v1

kind: Service

metadata:
  name: {{ .Values.service.mysqlmaster.name }}

spec:
  clusterIP: None

  selector:
{{ .Values.labels | toYaml | indent 4 }}

  ports:
{{ .Values.service.mysqlmaster.ports | toYaml | indent 4 }}