apiVersion: v1

kind: Service

metadata:
  name: {{ .Values.service.mysqlslave.name }}

spec:
  clusterIP: None

  selector:
{{ .Values.labels | toYaml | indent 4 }}

  ports:
{{ .Values.service.mysqlslave.ports | toYaml | indent 4 }}