apiVersion: apps/v1

kind: StatefulSet

metadata:
  name: {{ .Values.statefulset.mysqlmaster.name }}

  labels:
{{ .Values.labels | toYaml | indent 4 }}

spec:
  selector:
    matchLabels:
{{ .Values.labels | toYaml | indent 6 }}

  serviceName: {{ .Values.service.mysqlmaster.name }}

  replicas: {{ .Values.statefulset.mysqlmaster.replicas }}

  updateStrategy:
{{ .Values.statefulset.mysqlmaster.updateStrategy | toYaml | indent 4 }}

  template:
    metadata:
      labels:
{{ .Values.labels | toYaml | indent 8 }}

    spec:
      nodeSelector:
{{ .Values.statefulset.mysqlmaster.pod.nodeSelector | toYaml | indent 8 }}

      containers:
      - name: {{ .Values.statefulset.mysqlmaster.name }}

        image: "{{ .Values.statefulset.mysqlmaster.pod.container.image.repo }}:{{ .Values.statefulset.mysqlmaster.pod.container.image.version }}"

        imagePullPolicy: {{ .Values.statefulset.mysqlmaster.pod.container.image.pullPolicy }}

        ports:
        - containerPort: {{ .Values.statefulset.mysqlmaster.pod.container.port }}

        env:
{{ .Values.statefulset.mysqlmaster.pod.container.env | toYaml | indent 10 }}

        volumeMounts:
{{ .Values.statefulset.mysqlmaster.pod.container.volumeMounts | toYaml | indent 10 }}

  volumeClaimTemplates:
{{ .Values.statefulset.mysqlmaster.volumeClaimTemplates | toYaml | indent 4 }}